@startuml
'!include https://raw.githubusercontent.com/joaohf/OTP-PlantUML/master/OTP.puml
' uncomment the following line and comment the first to use locally
!include OTP.iuml

Supervisor(base, "Base", "one_for_one")
GenServer(base_server, "Base.Server")
Agent(base_report, "Base.Report")
TaskSupervisor(basee_supervisor, "Base.Supervisor")

Rel(expose, base_server)
Rel(expose, base_report)
Rel(expose, base_supervisor)

@enduml

